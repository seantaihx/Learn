'''
Create a set:
items = {"pen", "pencil", "eraser"}
	•	Check if "pen" is in the set.
	•	Clear the set using a method.
	•	Then check if the set is empty.
'''

items = {"pen", "pencil", "eraser"}


print("pen" in items)  
items.clear()

print(len(items) == 0)  
print(items)            
